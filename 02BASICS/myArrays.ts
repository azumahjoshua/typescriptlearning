const names: string[] = []

names.push("azumah")
names.push("ama")
type User = {
    name: string,
    isActive: boolean
}

const allUser: Array<User> = []

const MLModels: number[][] = [
    
]
allUser.push({name:"Ama",isActive:true})
